version: '3'
services:
  runner:
    image: codewars/node-runner
    volumes:
      - c:/dev/chynotestapp/server:/runner/www
    ports: 
      - "9002:9002"
    entrypoint: bash
    stdin_open: true
    tty: true
    
    #entrypoint : ["npm", "start", "index.js"]

   #docker run -it -p 9002:9002  --rm --entrypoint  bash -v c:/dev/chynotestapp/server:/runner/www chynorun
#ENTRYPOINT ["npm", "start", "index.js"]
 #   - ./lib:/runner/lib
   #   - ./examples:/runner/examples
   #   - ./frameworks:/runner/frameworks
   #   - ./test:/runner/test

    #entrypoint: bash
   # stdin_open: true
   # tty: true